---
title: "<Profile_Name>"
audience: public
last_updated: <YYYY-MM-DD>
last_updated_by: <archon-run-id | human-name>
related_tickets: [<JIRA-KEYS>]
related_docs: [README.md, ../sharing-model.md]
---

# `<Profile_Name>`

<!--
TEMPLATE: profile documentation. One file per profile, located at
`docs/security/profiles/<Profile_API_Name>.md`. Auto-generated by
`sf-discover-document-profiles` per ADR-0013.

State, not history. Describe what the profile grants TODAY. Per-ticket
change history lives in git log + Jira; architectural reasoning lives
in linked ADRs.
-->

## Identity

| | |
|---|---|
| **API name** | `<Profile_API_Name>` |
| **Label** | `<Display Name>` |
| **User license** | <Salesforce / Salesforce Platform / Customer Community / etc.> |
| **License count** *(if known)* | <users with this profile assigned> |
| **Origin** | Stock with customizations / Cloned from `<base>` / Custom |
| **Description** *(from XML, if present)* | <verbatim or "_Not set._"> |

## Persona

What role this profile represents in the business. 1-2 sentences. The generator infers from the name + permissions; engineers can override on re-run.

Example: _"Sales reps handling renewals end-to-end. Full CRUD on `Opportunity`, `Account`, `Contact`, and `Renewal__c`. No access to billing or admin functions."_

## Object permissions

Every standard or custom object this profile grants any access to. Standard objects with stock permissions are noted; only non-default settings are called out.

| Object | Read | Create | Edit | Delete | View All | Modify All | Notes |
|---|---|---|---|---|---|---|---|
| `Account` | ✓ | ✓ | ✓ | — | — | — | |
| `Opportunity` | ✓ | ✓ | ✓ | ✓ | — | — | |
| `<Custom__c>` | ✓ | ✓ | ✓ | — | — | — | |

Objects with zero access are not listed. If the profile has access to no custom objects, write `_No custom-object access._`

## Field-level security overrides

Only fields where this profile's FLS differs from the field's default (i.e., the field is hidden, or read-only when normally editable). Standard FLS is not enumerated.

| Object | Field | Default | This profile | Why |
|---|---|---|---|---|
| `Account` | `AnnualRevenue` | Read/Edit | **Read-only** | <reason if known; else "_unknown_"> |
| `Renewal__c` | `Internal_Margin__c` | Read/Edit | **Hidden** | <reason> |

If FLS matches defaults across the board, write `_No FLS overrides — uses default field visibility._`

## App access

Apps this profile can access.

- `<App_Name>` <if it's the default app, note "(default)">
- `<App_Name>`

If only the stock Salesforce app is accessible, write `_Stock Salesforce app only._`

## Tab settings

Non-default tab visibility. (Default Visible / Default Off / Hidden — only call out non-default.)

| Tab | This profile |
|---|---|
| `<Tab_Name>` | **Default Off** |
| `<Tab_Name>` | **Hidden** |

If all tabs use their default visibility, write `_All tabs use default visibility._`

## System permissions of note

Notable system permissions enabled or disabled relative to the profile's license tier.

- **`<Permission_Label>`** — Enabled / Disabled. <one-line reason if known>

Stock permissions for the license tier are not listed. If nothing notable, write `_No notable system permission overrides._`

## Apex class access

Apex classes this profile can execute (anonymous or via UI/REST).

- `<ClassName>`
- `<ClassName>`

If none beyond standard, write `_No engagement-specific Apex class access._`

## Visualforce page access

VF pages this profile can render.

- `<PageName>`

If none, write `_No engagement-specific VF page access._`

## Connected App access

Connected Apps this profile can use.

- `<App_Name>` <— for what integration / external client>

If none, write `_No engagement-specific Connected App access._`

## Typically assigned with

Permission Sets that are usually assigned alongside this profile in this engagement.

- [`<Permission_Set>`](../permission-sets/<Permission_Set>.md) — <one-line: what it adds>

If unknown or none, write `_None known; permission sets are assigned ad-hoc._`

## Related decisions

ADRs in [`../../decisions/`](../../decisions/) that govern this profile's posture.

- [`<NNNN-slug>`](../../decisions/<NNNN-slug>.md) — <one-line>

If none, write `_None._`
