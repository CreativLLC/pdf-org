---
title: "<Permission_Set_Name>"
audience: public
last_updated: <YYYY-MM-DD>
last_updated_by: <archon-run-id | human-name>
related_tickets: [<JIRA-KEYS>]
related_docs: [README.md, ../sharing-model.md]
---

# `<Permission_Set_Name>`

<!--
TEMPLATE: permission set documentation. One file per permission set,
located at `docs/security/permission-sets/<PS_API_Name>.md`.
Auto-generated by `sf-discover-document-permission-sets` per ADR-0013.

Permission sets are ADDITIVE — they grant permissions on top of a user's
base profile. This template focuses on what the PS adds; permissions
inherited from a profile are not duplicated here.
-->

## Identity

| | |
|---|---|
| **API name** | `<PS_API_Name>` |
| **Label** | `<Display Name>` |
| **License** *(if PS requires one)* | <e.g., Salesforce CPQ, Service Cloud User, none> |
| **Permission Set Group membership** *(if any)* | <Group names that include this PS> |
| **Origin** | Standard PS / Engagement-authored |
| **Description** *(from XML, if present)* | <verbatim or "_Not set._"> |

## Purpose

What capability this permission set grants. 1-2 sentences. Frame in terms of WHO needs this and WHY.

Example: _"Grants the ability to approve renewal discounts above 15% — assigned to regional sales managers and senior AEs only."_

## Object permissions added

Object access this PS adds on top of the base profile. Standard CRUD inherited from profile is not listed; only PS-granted access.

| Object | Read | Create | Edit | Delete | View All | Modify All | Notes |
|---|---|---|---|---|---|---|---|
| `<Object>` | ✓ | — | ✓ | — | — | — | <e.g., "edit-only for approval workflow"> |

If the PS grants no object-permission additions, write `_No object-permission additions — this PS is purely field-/app-/system-permission scoped._`

## Field-level security added

FLS that this PS enables (typically lifting a profile-default restriction).

| Object | Field | Default | This PS adds |
|---|---|---|---|
| `Renewal__c` | `Approved_Discount__c` | Hidden | **Read/Edit** |

If none, write `_No FLS additions._`

## App access added

Apps this PS adds visibility for.

- `<App_Name>`

If none, write `_No app additions._`

## Tab settings overrides

Non-default tab visibility this PS sets.

| Tab | This PS |
|---|---|
| `<Tab_Name>` | **Available** |

If none, write `_No tab visibility additions._`

## System permissions added

Notable system permissions this PS enables.

- **`<Permission_Label>`** — <one-line reason>

If none, write `_No system permissions added._`

## Custom Permissions granted

Custom Permissions this PS grants (used as feature flags from Apex/Flow).

- **`<Custom_Permission_Name>`** — <what feature it gates; link to where it's checked>

If none, write `_No Custom Permissions granted._`

## Apex class access added

- `<ClassName>`

If none, write `_No Apex class access additions._`

## Visualforce page access added

- `<PageName>`

If none, write `_No VF page access additions._`

## Connected App access added

- `<App_Name>`

If none, write `_No Connected App access additions._`

## Typical assignment pattern

Who in this engagement typically gets this PS, and how (manual / via Permission Set Group / via Permission Set License).

Example: _"Assigned via `Sales_Manager_PSG` permission set group. All users in the Sales Manager role inherit this PS through that group."_

If unknown, write `_Assignment pattern not documented; check Setup or the engagement's onboarding runbook._`

## Related decisions

ADRs in [`../../decisions/`](../../decisions/) that govern this PS's existence or scope.

- [`<NNNN-slug>`](../../decisions/<NNNN-slug>.md) — <one-line>

If none, write `_None._`
