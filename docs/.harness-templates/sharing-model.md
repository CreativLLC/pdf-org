---
title: Sharing model
audience: public
last_updated: <YYYY-MM-DD>
last_updated_by: <archon-run-id | human-name>
related_tickets: [<JIRA-KEYS>]
related_docs: [README.md, ../decisions/]
---

# Sharing model

<!--
TEMPLATE: the consolidated sharing-posture doc, auto-generated by
`sf-discover-document-security-overview` per ADR-0013. Lives at
`docs/security/sharing-model.md`.

Audience: compliance / audit readers, onboarding engineers, AI agents
loading docs as context. Plain-English summary of how access works
org-wide; per-profile and per-permission-set detail lives in sibling
files under `docs/security/profiles/` and `docs/security/permission-sets/`.
-->

## Org-wide defaults (OWD)

Every object's default record visibility, before sharing rules / Apex sharing / role hierarchy.

| Object | Internal OWD | External OWD | Grant Access Using Hierarchies |
|---|---|---|---|
| `<Object>` | Public Read/Write / Public Read Only / Private / Controlled by Parent | <if applicable> | Yes / No |
| `<Object>` | … | … | … |

> The full list is in each object's [`../objects/<Object>.md`](../objects/) "Sharing model" section. This table is the rolled-up view.

## Sharing rules

Configured rules that grant access beyond OWD. Listed per object that has any.

### `<Object>`

| Rule name | Type | Criterion | Shared with | Access level |
|---|---|---|---|---|
| `<Rule_Name>` | Criteria-based / Ownership-based | <plain English of the criterion> | <Public Group / Role / Role+Subordinates / Queue> | Read / Read/Write |

If no objects have custom sharing rules, write `_None — this engagement relies on OWD + role hierarchy + Apex sharing only._`

## Apex (programmatic) sharing

Where this engagement's Apex code creates `__Share` records programmatically.

| Apex class | Method | Object shared | Triggered by | Access level |
|---|---|---|---|---|
| `<ClassName>` | `<methodName>` | `<Object>__Share` | <event/trigger description> | Read / Edit |

If no Apex sharing is used, write `_None — all sharing is declarative._`

## Implicit sharing notes

Standard Salesforce implicit sharing that affects this engagement:

- **Account → Contact**: Read access to an Account grants Read to its Contacts (when OWD allows).
- **Account → Opportunity**: same pattern (if Opportunities are used).
- **Parent-child custom relationships**: list any Master-Detail relationships where the child object's OWD is "Controlled by Parent."

Write `_Standard Salesforce implicit sharing applies; no engagement-specific notes._` if nothing custom.

## Restriction Rules

Restriction Rules (newer Salesforce feature) further limit what users see, regardless of sharing.

| Object | Rule name | Condition | Restricts |
|---|---|---|---|
| `<Object>` | `<Rule_Name>` | <plain English> | <what's hidden and from whom> |

If no Restriction Rules exist, write `_None._`

## Role hierarchy notes

High-level summary only; the full hierarchy is configured in Setup.

- **Depth**: <N> levels.
- **Key roles**: <list the 3-5 most significant by name>.
- **Cross-functional access**: any roles that span multiple departments.
- **External roles** (Partner / Community users): any in use.

If the engagement uses a flat or near-flat hierarchy, say so.

## Custom Permissions usage map

Custom Permissions are feature-flag-style entitlements checked from Apex or Flow. See [`custom-permissions.md`](./custom-permissions.md) for the per-permission detail. This section lists which profiles / permission sets grant each.

| Custom Permission | Granted by |
|---|---|
| `<Permission_Name>` | <Profile names / Permission Set names> |

If no Custom Permissions exist, write `_None — see [`custom-permissions.md`](./custom-permissions.md) for context._`

## Public Groups and Queues

See [`public-groups-and-queues.md`](./public-groups-and-queues.md) for full enumeration. This section summarizes which groups/queues participate in the sharing model.

- **Groups used in sharing rules**: list.
- **Queues**: list with what each queues (e.g., "Case queue for Tier 2 routing").

## Why this posture (engagement narrative)

A 2-4 paragraph hand-authored explanation of WHY the engagement's sharing posture is what it is. The auto-generator stubs this section with a placeholder; engineers fill in for their engagement.

Typical content:
- Business reason for the OWD choices ("Renewals are commercially sensitive, hence Private OWD with account-team sharing").
- Why certain sharing rules exist ("Cross-region visibility for the Pricing Council is granted via the `Pricing_Council_RW` rule on `Renewal__c`").
- Major decisions deferred or rejected (e.g., "We considered role-hierarchy-based visibility for renewals but rejected it because regional VPs shouldn't have automatic access to peer regions' deals — see [ADR-NNNN](../decisions/)").

If this section is still the placeholder after the first hand-edit pass, mark it `_TODO: engineer to author._`

## Related decisions

ADRs in [`../decisions/`](../decisions/) that govern this sharing posture.

- [`<NNNN-slug>`](../decisions/<NNNN-slug>.md) — <one-line summary>

If none, write `_None._`
